protobuf-net MonoTouch example. Uses self compiled v2 beta r470 assembly on iOS and 
pre-built r450 assemblies for the Windows side. All assemblies are under the
Assemblies folder.

The serialization assembly is created using solution in Windows. It gets saved
to Assemblies/Any folder and is under source code control.
